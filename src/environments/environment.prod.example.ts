/**
 * Archivo de ejemplo para variables de entorno de producción
 * 
 * INSTRUCCIONES:
 * 1. Copia este archivo como 'environment.prod.ts'
 * 2. Reemplaza los valores de ejemplo con tus valores reales de producción
 * 3. NO subas 'environment.prod.ts' al repositorio (está en .gitignore)
 */
export const environment = {
  production: true,
  apiBaseUrl: 'TU_URL_API_PRODUCCION_AQUI'
};
